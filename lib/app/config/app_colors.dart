import 'package:flutter/material.dart';

abstract class AppColors {
  // === Main Color ===
  static const Color primary = Color(0xFF0A400C);

  // === Gradient Colors ===
  static const Color gradientStart = Color(0xFF859F3D);
  static const Color gradientEnd = Color(0xFF31511E);
}
